import { DateCompactPipe } from './date-compact.pipe';

describe('DateCompactPipe', () => {
  it('create an instance', () => {
    const pipe = new DateCompactPipe();
    expect(pipe).toBeTruthy();
  });
});
